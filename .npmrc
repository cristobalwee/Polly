# Expo + Metro resolve a flat node_modules more reliably than pnpm's
# default isolated layout, so we use the hoisted linker for this monorepo.
node-linker=hoisted

# Tamagui RC + Expo SDK 55 have a few benign peer-range mismatches;
# don't let those hard-fail installs.
strict-peer-dependencies=false
